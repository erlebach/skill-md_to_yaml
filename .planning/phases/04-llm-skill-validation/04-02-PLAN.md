---
phase: 04-llm-skill-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/erlebach/src/2026/claude-code/n8n/md_to_yaml/tests/test_skill_output.py
  - /Users/erlebach/src/2026/claude-code/n8n/md_to_yaml/tests/fixtures/skill_output_sample.yaml
autonomous: true
requirements: [LLM-02, LLM-03]

must_haves:
  truths:
    - "Test fixtures exist with sample LLM-generated YAML covering multiple layout types"
    - "Tests verify YAML validates against Pydantic parser"
    - "Tests verify layout variety (no 3+ consecutive same layouts)"
    - "Tests verify alt_text present on diagram/figure slides"
  artifacts:
    - path: "tests/test_skill_output.py"
      provides: "Unit tests for skill output validation"
      min_lines: 50
    - path: "tests/fixtures/skill_output_sample.yaml"
      provides: "Sample YAML fixture representing realistic LLM output"
      min_lines: 40
  key_links:
    - from: "tests/test_skill_output.py"
      to: "schema/parser.py"
      via: "import parse_deck_file"
      pattern: "from schema.parser import parse_deck_file"
    - from: "tests/test_skill_output.py"
      to: "compiler/validators.py"
      via: "import validate_deck"
      pattern: "from compiler.validators import validate_deck"
---

<objective>
Create test infrastructure that validates LLM-generated YAML output meets schema and design constraints.

Purpose: Provides automated verification that skill output satisfies LLM-02 (design constraints) and LLM-03 (schema validation) without requiring full E2E runs.
Output: Test file + fixture YAML file
</objective>

<execution_context>
@/Users/erlebach/.claude/get-shit-done/workflows/execute-plan.md
@/Users/erlebach/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-llm-skill-validation/04-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create sample YAML fixture and validation tests</name>
  <files>
    /Users/erlebach/src/2026/claude-code/n8n/md_to_yaml/tests/fixtures/skill_output_sample.yaml
    /Users/erlebach/src/2026/claude-code/n8n/md_to_yaml/tests/test_skill_output.py
  </files>
  <read_first>
    /Users/erlebach/src/2026/claude-code/n8n/md_to_yaml/schema/models.py
    /Users/erlebach/src/2026/claude-code/n8n/md_to_yaml/schema/parser.py
    /Users/erlebach/src/2026/claude-code/n8n/md_to_yaml/compiler/validators.py
    /Users/erlebach/src/2026/claude-code/n8n/md_to_yaml/tests/conftest.py
  </read_first>
  <behavior>
    - test_fixture_validates: parse_deck_file("tests/fixtures/skill_output_sample.yaml") returns a Deck with 10+ slides and no errors
    - test_layout_variety: no 3+ consecutive slides with same layout type in fixture
    - test_alt_text_present: all diagram and figure slides in fixture have non-empty alt_text
    - test_no_body_in_frontmatter: fixture parses without "body field in YAML frontmatter" error
    - test_all_layout_types_used: fixture uses at least 6 different layout types (demonstrating variety)
    - test_validate_deck_no_errors: validate_deck(deck) returns empty errors list
    - test_bad_yaml_body_in_frontmatter: a YAML string with body: in frontmatter raises ValueError
    - test_bad_yaml_unknown_layout: a YAML string with layout: bullet-list raises ValidationError
  </behavior>
  <action>
**Step 1: Create fixture file** `tests/fixtures/skill_output_sample.yaml`

Create a realistic 12-slide deck simulating LLM output for a "Density-Based Clustering" topic. Must include:

```yaml
---
title: Density-Based Clustering Methods
author: Research Team
date: 2026-03-22
theme: dark
font: IBM Plex Sans
---

---
layout: title
title: Density-Based Clustering
subtitle: DBSCAN, OPTICS, and Beyond
---

---
layout: hero
title: Why Density-Based Methods?
---
Traditional partitioning methods like K-Means assume spherical clusters. Density-based methods discover clusters of **arbitrary shape** by identifying regions of high point density separated by low-density regions.

---
layout: content
title: Core Concepts
---
- **Epsilon (ε)**: Radius defining the neighborhood of a point
- **MinPts**: Minimum number of points required to form a dense region
- **Core point**: Has ≥ MinPts neighbors within ε-radius
- **Border point**: Within ε of a core point but fewer than MinPts neighbors

---
layout: diagram
title: DBSCAN Point Classification
alt_text: "Flowchart showing how DBSCAN classifies points as core, border, or noise based on epsilon and MinPts parameters"
---
graph TD
  A[Input Point p] --> B{Count neighbors in ε-radius}
  B --> C{≥ MinPts?}
  C -->|Yes| D[Core Point]
  C -->|No| E{Near a core point?}
  E -->|Yes| F[Border Point]
  E -->|No| G[Noise Point]

---
layout: two-column
title: DBSCAN vs OPTICS
proportion: 50/50
left:
  type: text
---
**DBSCAN**: Fixed ε parameter, binary density threshold. Works well when clusters have similar density.

**OPTICS**: Variable density detection, produces reachability plot. Handles clusters with varying density levels.

---
layout: steps
title: DBSCAN Algorithm Steps
steps:
  - label: Initialize
    detail: Mark all points as unvisited
  - label: Select Point
    detail: Pick an unvisited point p
  - label: Expand Cluster
    detail: If p is core, create cluster and add all density-reachable points
  - label: Repeat
    detail: Continue until all points visited

---
layout: code
title: DBSCAN Implementation
language: python
---
from sklearn.cluster import DBSCAN
import numpy as np

X = np.array([[1, 2], [2, 2], [2, 3], [8, 7], [8, 8], [25, 80]])
clustering = DBSCAN(eps=3, min_samples=2).fit(X)
print(clustering.labels_)  # [-1, 0, 0, 1, 1, -1]

---
layout: comparison
title: Method Comparison
---
**Advantages**
- No need to specify number of clusters
- Finds arbitrarily shaped clusters
- Robust to outliers (labels them as noise)
- Deterministic results

<!-- split -->

**Limitations**
- Sensitive to ε and MinPts parameters
- Struggles with varying density clusters
- High dimensionality reduces effectiveness
- Not suitable for very large datasets without indexing

---
layout: table
title: Performance Benchmarks
caption: "Clustering accuracy on synthetic datasets"
headers: [Method, Moons, Circles, Blobs, Aniso]
rows:
  - [DBSCAN, "98.5%", "99.1%", "95.2%", "97.3%"]
  - [K-Means, "72.1%", "51.3%", "99.5%", "68.4%"]
  - [OPTICS, "97.8%", "98.7%", "94.1%", "96.9%"]

---
layout: quote
title: Key Insight
attribution: "Ester et al., KDD 1996"
---
A cluster is defined as a maximal set of density-connected points.

---
layout: summary
title: Key Takeaways
---
- Density-based clustering discovers arbitrary-shape clusters without specifying k
- DBSCAN is the foundational algorithm; OPTICS extends it for varying densities
- Parameter selection (ε, MinPts) is critical — use k-distance graphs for guidance
- Best suited for spatial data and datasets with noise
```

**Step 2: Create test file** `tests/test_skill_output.py`

```python
"""Tests validating LLM-generated YAML output meets schema and design constraints."""
import pytest
from pathlib import Path
from schema.parser import parse_deck_file
from compiler.validators import validate_deck

FIXTURE = Path(__file__).parent / "fixtures" / "skill_output_sample.yaml"

def test_fixture_validates():
    deck = parse_deck_file(str(FIXTURE))
    assert len(deck.slides) >= 10

def test_layout_variety():
    deck = parse_deck_file(str(FIXTURE))
    layouts = [s.layout for s in deck.slides]
    for i in range(len(layouts) - 2):
        assert not (layouts[i] == layouts[i+1] == layouts[i+2]), \
            f"3+ consecutive '{layouts[i]}' at positions {i}-{i+2}"

def test_alt_text_present():
    deck = parse_deck_file(str(FIXTURE))
    for i, slide in enumerate(deck.slides):
        if slide.layout in ("diagram", "figure"):
            assert hasattr(slide, 'alt_text') and slide.alt_text, \
                f"Slide {i+1} ({slide.layout}) missing alt_text"

def test_all_layout_types_variety():
    deck = parse_deck_file(str(FIXTURE))
    unique_layouts = set(s.layout for s in deck.slides)
    assert len(unique_layouts) >= 6, f"Only {len(unique_layouts)} unique layouts: {unique_layouts}"

def test_validate_deck_no_errors():
    deck = parse_deck_file(str(FIXTURE))
    errors, warnings = validate_deck(deck)
    assert len(errors) == 0, f"Validation errors: {errors}"

def test_no_body_in_frontmatter():
    """Fixture parses without body-in-frontmatter error (implicit — parse_deck_file would raise)."""
    deck = parse_deck_file(str(FIXTURE))
    assert deck is not None

def test_bad_yaml_body_in_frontmatter(tmp_path):
    bad_yaml = tmp_path / "bad.yaml"
    bad_yaml.write_text("""---
title: Test
author: Test
date: 2026-01-01
theme: dark
---

---
layout: content
title: Bad Slide
body: "This should not be here"
---
""")
    with pytest.raises((ValueError, Exception)):
        parse_deck_file(str(bad_yaml))

def test_bad_yaml_unknown_layout(tmp_path):
    bad_yaml = tmp_path / "bad2.yaml"
    bad_yaml.write_text("""---
title: Test
author: Test
date: 2026-01-01
theme: dark
---

---
layout: bullet-list
title: Unknown Layout
---
- item 1
""")
    with pytest.raises(Exception):
        parse_deck_file(str(bad_yaml))
```
  </action>
  <verify>
    <automated>cd /Users/erlebach/src/2026/claude-code/n8n/md_to_yaml && python3 -m pytest tests/test_skill_output.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/fixtures/skill_output_sample.yaml exists with 12 slides covering 8+ layout types
    - tests/test_skill_output.py exists with 8 test functions
    - All 8 tests pass: test_fixture_validates, test_layout_variety, test_alt_text_present, test_all_layout_types_variety, test_validate_deck_no_errors, test_no_body_in_frontmatter, test_bad_yaml_body_in_frontmatter, test_bad_yaml_unknown_layout
    - grep "from schema.parser import parse_deck_file" tests/test_skill_output.py returns match
    - grep "from compiler.validators import validate_deck" tests/test_skill_output.py returns match
  </acceptance_criteria>
  <done>Test suite validates that LLM-quality YAML output passes schema validation, design constraints, and rejects common LLM errors</done>
</task>

</tasks>

<verification>
All tests pass: `cd /Users/erlebach/src/2026/claude-code/n8n/md_to_yaml && python3 -m pytest tests/test_skill_output.py -v`
</verification>

<success_criteria>
- Sample fixture proves the 12-layout YAML format works end-to-end through parser
- Tests catch the most common LLM errors (body in frontmatter, unknown layouts)
- validate_deck() confirms no design constraint violations
</success_criteria>

<output>
After completion, create `.planning/phases/04-llm-skill-validation/04-02-SUMMARY.md`
</output>
