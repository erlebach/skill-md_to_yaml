---
phase: 04-llm-skill-validation
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - /Users/erlebach/src/2026/claude-code/n8n/md_to_yaml/tests/e2e_validation/density_based.yaml
  - /Users/erlebach/src/2026/claude-code/n8n/md_to_yaml/tests/e2e_validation/prototype_based.yaml
  - /Users/erlebach/src/2026/claude-code/n8n/md_to_yaml/tests/e2e_validation/quantum_transformers.yaml
autonomous: false

requirements: [LLM-01, LLM-02, LLM-03]

must_haves:
  truths:
    - "3 real PDFs are converted to YAML DSL following the skill instructions"
    - "All 3 YAML files pass parse_deck_file() validation"
    - "All 3 YAML files compile to HTML via python3 -m compiler"
    - "Compiled HTML has ADA compliance markers (ARIA, skip links, alt text)"
    - "Human confirms layout variety and professional appearance"
  artifacts:
    - path: "tests/e2e_validation/density_based.yaml"
      provides: "YAML DSL for density-based clustering deck"
      min_lines: 50
    - path: "tests/e2e_validation/prototype_based.yaml"
      provides: "YAML DSL for prototype-based clustering deck"
      min_lines: 50
    - path: "tests/e2e_validation/quantum_transformers.yaml"
      provides: "YAML DSL for quantum transformers deck"
      min_lines: 50
  key_links:
    - from: "tests/e2e_validation/*.yaml"
      to: "schema/parser.py"
      via: "parse_deck_file validation"
      pattern: "parse_deck_file"
    - from: "tests/e2e_validation/*.yaml"
      to: "compiler/__main__.py"
      via: "python3 -m compiler compilation"
      pattern: "python3 -m compiler"
---

<objective>
Run end-to-end validation: generate YAML DSL from 3 real PDF sources following the skill's instructions, validate, compile to HTML, and human spot-check the results.

Purpose: Proves the complete pipeline works on real-world academic content — the ultimate test of the skill + schema + compiler integration.
Output: 3 validated YAML files, 3 compiled HTML files, human verification of quality
</objective>

<execution_context>
@/Users/erlebach/.claude/get-shit-done/workflows/execute-plan.md
@/Users/erlebach/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-llm-skill-validation/04-CONTEXT.md
@.planning/phases/04-llm-skill-validation/04-01-SUMMARY.md
@.planning/phases/04-llm-skill-validation/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate and validate YAML for 3 real PDFs</name>
  <files>
    /Users/erlebach/src/2026/claude-code/n8n/md_to_yaml/tests/e2e_validation/density_based.yaml
    /Users/erlebach/src/2026/claude-code/n8n/md_to_yaml/tests/e2e_validation/prototype_based.yaml
    /Users/erlebach/src/2026/claude-code/n8n/md_to_yaml/tests/e2e_validation/quantum_transformers.yaml
  </files>
  <read_first>
    /Users/erlebach/.claude/skills/md_to_yaml/SKILL.md
    /Users/erlebach/src/2026/claude-code/n8n/md_to_yaml/layout_rules_llm.md
    /Users/erlebach/.claude/skills/ada-slides-general/accessibility-core.md
    /Users/erlebach/.claude/skills/ada-slides-general/flavor-explanatory.md
  </read_first>
  <action>
Follow the SKILL.md instructions manually for each of the 3 PDFs. Create directory first: `mkdir -p tests/e2e_validation`

**PDF 1: Density-Based Clustering** (explanatory flavor)
- Source: `../n8n_to_python/slides_density-based.pdf`
- Read the PDF with Read tool to extract content
- Generate YAML DSL following SKILL.md steps 6-7, using explanatory flavor guidance
- Write to `tests/e2e_validation/density_based.yaml`
- Validate: `cd /Users/erlebach/src/2026/claude-code/n8n/md_to_yaml && python3 -c "from schema.parser import parse_deck_file; deck = parse_deck_file('tests/e2e_validation/density_based.yaml'); print('VALID:', len(deck.slides), 'slides')"`
- If errors, fix and retry (up to 3 attempts per SKILL.md)
- Check validators: `python3 -c "from schema.parser import parse_deck_file; from compiler.validators import validate_deck; deck = parse_deck_file('tests/e2e_validation/density_based.yaml'); errors, warnings = validate_deck(deck); print('Errors:', errors); print('Warnings:', warnings)"`
- Compile: `python3 -m compiler tests/e2e_validation/density_based.yaml tests/e2e_validation/density_based.html --embed-images`

**PDF 2: Prototype-Based Clustering** (implementation flavor)
- Source: `../n8n_to_python/slides_prototype-based.pdf`
- Read PDF, generate YAML with implementation flavor guidance
- Read `/Users/erlebach/.claude/skills/ada-slides-general/flavor-implementation.md` for this one
- Write to `tests/e2e_validation/prototype_based.yaml`
- Validate and compile same as above, output to `tests/e2e_validation/prototype_based.html`

**PDF 3: Quantum Transformers** (explanatory flavor)
- Source: `../n8n_to_python/A Survey of Quantum Transformers- Architectures, Challenges and Outlooks_nov2025_zhang etal_review.pdf`
- Read PDF, generate YAML with explanatory flavor
- Write to `tests/e2e_validation/quantum_transformers.yaml`
- Validate and compile same as above, output to `tests/e2e_validation/quantum_transformers.html`

**Requirements for each YAML file:**
- 15-25 slides (appropriate for academic content)
- Uses at least 6 different layout types
- No 3+ consecutive same layout types
- All diagram/figure slides have descriptive alt_text
- Math notation preserved as $$...$$ where present in source
- Mermaid diagrams use raw syntax (no triple-backtick fences)
- body content BELOW closing --- (never in YAML frontmatter)

**Final validation sweep:**
```bash
cd /Users/erlebach/src/2026/claude-code/n8n/md_to_yaml
for f in tests/e2e_validation/*.yaml; do
  echo "=== $f ==="
  python3 -c "from schema.parser import parse_deck_file; from compiler.validators import validate_deck; deck = parse_deck_file('$f'); errors, warnings = validate_deck(deck); print(f'Slides: {len(deck.slides)}, Errors: {len(errors)}, Warnings: {len(warnings)}')"
done
```
  </action>
  <verify>
    <automated>cd /Users/erlebach/src/2026/claude-code/n8n/md_to_yaml && python3 -c "
from schema.parser import parse_deck_file
from compiler.validators import validate_deck
import os
for f in ['tests/e2e_validation/density_based.yaml', 'tests/e2e_validation/prototype_based.yaml', 'tests/e2e_validation/quantum_transformers.yaml']:
    deck = parse_deck_file(f)
    errors, warnings = validate_deck(deck)
    html = f.replace('.yaml', '.html')
    assert os.path.exists(html), f'Missing HTML: {html}'
    assert len(errors) == 0, f'{f}: {errors}'
    print(f'{f}: {len(deck.slides)} slides, {len(errors)} errors, {len(warnings)} warnings, HTML exists')
print('ALL 3 PASS')
"</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e_validation/density_based.yaml exists and passes parse_deck_file()
    - tests/e2e_validation/prototype_based.yaml exists and passes parse_deck_file()
    - tests/e2e_validation/quantum_transformers.yaml exists and passes parse_deck_file()
    - tests/e2e_validation/density_based.html exists (compiled)
    - tests/e2e_validation/prototype_based.html exists (compiled)
    - tests/e2e_validation/quantum_transformers.html exists (compiled)
    - Each YAML has 15+ slides
    - validate_deck() returns 0 errors for all 3
    - Each HTML file contains "aria-roledescription" (ARIA carousel)
    - Each HTML file contains "skip-link" (skip navigation)
  </acceptance_criteria>
  <done>All 3 PDFs converted to validated YAML, compiled to ADA-compliant HTML with zero validation errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human spot-check compiled HTML decks</name>
  <files>tests/e2e_validation/density_based.html, tests/e2e_validation/prototype_based.html, tests/e2e_validation/quantum_transformers.html</files>
  <action>
Open all 3 compiled HTML files in browser for human review:
- `open tests/e2e_validation/density_based.html`
- `open tests/e2e_validation/prototype_based.html`
- `open tests/e2e_validation/quantum_transformers.html`

Present to user for visual verification of layout variety, keyboard navigation, readability, and professional appearance.
  </action>
  <what-built>3 complete HTML slide decks generated from real PDF sources via the YAML DSL pipeline</what-built>
  <how-to-verify>
    1. Open each HTML file in browser (commands above)
    2. For each deck, verify:
       a. Slides advance with arrow keys (keyboard navigation works)
       b. Layout variety is visible (not all content slides — mix of diagrams, two-column, code, etc.)
       c. Text is readable (contrast is sufficient)
       d. Professional appearance comparable to existing decks in ../n8n_to_python/output_html_files/
       e. Math notation renders correctly (if present)
       f. Mermaid diagrams render (if present)
    3. Optional: Run browser accessibility audit (F12 -> Lighthouse -> Accessibility)
  </how-to-verify>
  <verify>Human approval of all 3 HTML decks</verify>
  <done>User types "approved" confirming all 3 decks are professional and functional</done>
  <resume-signal>Type "approved" if all 3 decks look professional and functional, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
- 3 YAML files validate with zero errors
- 3 HTML files compile successfully
- Human confirms visual quality and ADA compliance
- Full test suite still passes: `python3 -m pytest tests/ -x -q`
</verification>

<success_criteria>
- End-to-end pipeline proven on 3 real academic PDFs
- Generated HTML is ADA-compliant and visually professional
- Human approves quality comparable to existing /ada-slides-general output
</success_criteria>

<output>
After completion, create `.planning/phases/04-llm-skill-validation/04-03-SUMMARY.md`
</output>
