---
phase: 02-compiler-core-ada
plan: 04
type: execute
wave: 3
depends_on: [02-03]
files_modified:
  - tests/test_compiler.py
autonomous: false
requirements: [COMP-01, COMP-02, COMP-03, COMP-04, COMP-05, COMP-06, ADA-01, ADA-02, ADA-03, ADA-04, ADA-05, ADA-06, ADA-07, ADA-08]
must_haves:
  truths:
    - "All 5 real-world YAML fixtures compile to valid HTML without errors"
    - "Real-world fixture output passes all ARIA and structural checks"
    - "User confirms visual quality of compiled output in browser"
  artifacts:
    - path: "tests/test_compiler.py"
      provides: "Real-fixture integration tests added"
  key_links:
    - from: "tests/test_compiler.py"
      to: "tests/fixtures/real_*.yaml"
      via: "parametrized test loading each fixture"
      pattern: "real_.*\\.yaml"
---

<objective>
Run the full compiler against all 5 real-world YAML fixtures from Phase 01.1, verify output, and do a visual spot-check.

Purpose: End-to-end validation that the compiler handles real-world content, not just minimal test decks.
Output: Passing integration tests on real fixtures + user visual confirmation.
</objective>

<execution_context>
@/Users/erlebach/.claude/get-shit-done/workflows/execute-plan.md
@/Users/erlebach/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-compiler-core-ada/02-CONTEXT.md
@.planning/phases/02-compiler-core-ada/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Real-fixture integration tests</name>
  <files>tests/test_compiler.py</files>
  <read_first>
    - tests/test_compiler.py (existing tests — append to this file)
    - tests/fixtures/real_quantum_transformers_impl.yaml (sample fixture to understand structure)
    - compiler/engine.py (compile_deck signature)
  </read_first>
  <action>
Add to `tests/test_compiler.py`:

1. `REAL_FIXTURES` list: glob `Path('tests/fixtures').glob('real_*.yaml')` — should find 5 files.

2. `test_real_fixtures_exist()` — assert len(REAL_FIXTURES) == 5.

3. `@pytest.mark.parametrize("fixture", REAL_FIXTURES, ids=lambda p: p.stem)` on `test_real_fixture_compiles(fixture, tmp_path)`:
   - `from schema.parser import parse_deck_file`
   - `deck = parse_deck_file(str(fixture))`
   - `out = tmp_path / f"{fixture.stem}.html"`
   - `compile_deck(deck, str(out))`
   - assert out.exists()
   - content = out.read_text()
   - assert '<!DOCTYPE html>' in content
   - assert 'aria-roledescription="carousel"' in content
   - assert 'class="skip-link"' in content
   - assert content.count('role="group"') == len(deck.slides)

4. `test_real_fixture_deterministic(tmp_path)` — pick first fixture, compile twice, assert identical output.

5. Compile all 5 fixtures to `/tmp/compiler_test_output/` for visual inspection in the checkpoint task:
   `test_compile_all_for_visual_check(tmp_path)` — for each fixture, compile to `tmp_path / f"{stem}.html"`. This test just ensures no crashes; the checkpoint verifies visually.
  </action>
  <verify>
    <automated>cd /Users/erlebach/src/2026/claude-code/n8n/md_to_yaml && python -m pytest tests/test_compiler.py -x -v -k "real"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_compiler.py contains `test_real_fixture_compiles` parametrized over 5 fixtures
    - tests/test_compiler.py contains `test_real_fixture_deterministic`
    - `python -m pytest tests/test_compiler.py -x -k "real"` exits 0
    - All 5 real fixtures compile without errors
  </acceptance_criteria>
  <done>All 5 real-world fixtures compile successfully and pass structural checks</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual spot-check of compiled output</name>
  <files>none</files>
  <action>
Compile all 5 real-world fixtures to /tmp/compiler_visual/ for user to open in browser:
`mkdir -p /tmp/compiler_visual && for f in tests/fixtures/real_*.yaml; do python -m compiler "$f" "/tmp/compiler_visual/$(basename ${f%.yaml}.html)"; done`
  </action>
  <what-built>Complete compiler pipeline: 5 real-world YAML decks compiled to ADA-compliant HTML with dark theme, keyboard navigation, ARIA carousel markup, skip links, and slide counter.</what-built>
  <how-to-verify>
    1. Run: `cd /Users/erlebach/src/2026/claude-code/n8n/md_to_yaml && mkdir -p /tmp/compiler_visual && for f in tests/fixtures/real_*.yaml; do python -m compiler "$f" "/tmp/compiler_visual/$(basename ${f%.yaml}.html)"; done`
    2. Open `/tmp/compiler_visual/real_quantum_transformers_impl.html` in browser
    3. Verify: Dark background, amber accents, readable text, slide counter in bottom-right
    4. Press ArrowRight — slide advances; ArrowLeft — retreats; Home — first slide; End — last
    5. Check: Each slide has a visible heading
    6. Open a second file (e.g., real_density_clustering.html) for variety
    7. Tab to skip link — should appear on focus
  </how-to-verify>
  <verify>User visually confirms output quality in browser</verify>
  <done>User types "approved" confirming visual quality, keyboard nav, and ARIA structure</done>
  <resume-signal>Type "approved" or describe visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -x` — full suite green
- Visual confirmation of output quality
</verification>

<success_criteria>
- All 5 real-world fixtures compile without errors
- Output HTML visually matches target quality (dark theme, amber accents, readable)
- Keyboard navigation works in browser
- User approves visual output
</success_criteria>

<output>
After completion, create `.planning/phases/02-compiler-core-ada/02-04-SUMMARY.md`
</output>
