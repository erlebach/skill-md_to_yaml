---
phase: 02-compiler-core-ada
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_compiler.py
  - tests/test_validators.py
  - tests/fixtures/minimal_deck.yaml
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03, COMP-04, COMP-05, COMP-06, ADA-01, ADA-02, ADA-03, ADA-04, ADA-05, ADA-06, ADA-07, ADA-08]
must_haves:
  truths:
    - "Test stub files exist and are importable by pytest"
    - "All 14 test functions are present as xfail stubs"
    - "A minimal deck fixture exists for compiler tests"
  artifacts:
    - path: "tests/test_compiler.py"
      provides: "xfail stubs for COMP-01 through COMP-06, ADA-02 through ADA-05"
    - path: "tests/test_validators.py"
      provides: "xfail stubs for ADA-01, ADA-06, ADA-07, ADA-08"
    - path: "tests/fixtures/minimal_deck.yaml"
      provides: "Minimal valid deck for compiler integration tests"
  key_links: []
---

<objective>
Create Wave 0 test stubs for all Phase 2 test functions, satisfying the Nyquist requirement that test files exist before implementation begins.

Purpose: VALIDATION.md requires tests/test_compiler.py and tests/test_validators.py to exist with stub functions before Wave 1 plans execute. This ensures every task's `<verify>` command has a target file.
Output: Two test files with xfail stubs and one minimal YAML fixture.
</objective>

<execution_context>
@/Users/erlebach/.claude/get-shit-done/workflows/execute-plan.md
@/Users/erlebach/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-compiler-core-ada/02-CONTEXT.md
@.planning/phases/02-compiler-core-ada/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stubs and minimal fixture</name>
  <files>tests/test_compiler.py, tests/test_validators.py, tests/fixtures/minimal_deck.yaml</files>
  <read_first>
    - tests/conftest.py (existing test infrastructure from Phase 1)
    - schema/models.py (model names needed for imports)
    - pyproject.toml (pytest config)
  </read_first>
  <action>
1. Create `tests/fixtures/minimal_deck.yaml` — a minimal valid deck:
   ```yaml
   ---
   title: Test Deck
   theme: dark
   ---
   # Title Slide
   layout: title
   subtitle: Subtitle
   ---
   ```

2. Create `tests/test_compiler.py` with xfail stubs for 10 functions matching VALIDATION.md task IDs. Each function is decorated with `@pytest.mark.xfail(reason="stub — implementation in Plan 03")` and has `assert False` as the body:
   - `test_compile_produces_html` (COMP-01)
   - `test_all_layout_types_render` (COMP-02)
   - `test_deterministic_output` (COMP-03)
   - `test_cli_invocation` (COMP-04)
   - `test_css_vars_embedded` (COMP-05)
   - `test_keyboard_js_embedded` (COMP-06, ADA-05)
   - `test_aria_carousel_markup` (ADA-02)
   - `test_skip_link_present` (ADA-03)
   - `test_aria_labelledby` (ADA-04)
   - `test_cli_error_exit_code` (error path)

3. Create `tests/test_validators.py` with xfail stubs for 8 functions:
   - `test_missing_alt_text_error` (ADA-01)
   - `test_valid_alt_text_no_error`
   - `test_invalid_contrast_error` (ADA-06)
   - `test_valid_contrast_no_error`
   - `test_no_accent_color_no_error`
   - `test_layout_variety_warning` (ADA-07)
   - `test_no_layout_variety_warning_for_two`
   - `test_bullet_limit_warning` (ADA-08)
   - `test_bullet_limit_ok_for_five`
   - `test_contrast_dark_theme_amber_passes`

All stubs use `import pytest` and `@pytest.mark.xfail(reason="stub")`. No production imports — stubs must pass collection even before compiler/ package exists.
  </action>
  <verify>
    <automated>cd /Users/erlebach/src/2026/claude-code/n8n/md_to_yaml && python -m pytest tests/test_compiler.py tests/test_validators.py --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <done>pytest collects all 20 test stubs, all marked xfail, minimal_deck.yaml fixture exists</done>
</task>

</tasks>

<verification>
- `pytest tests/test_compiler.py tests/test_validators.py --collect-only` collects 20 items
- `pytest tests/test_compiler.py tests/test_validators.py -x` runs with 20 xfail (no errors)
- `tests/fixtures/minimal_deck.yaml` exists and is valid YAML
</verification>

<success_criteria>
- Both test files exist and are importable
- All stub functions match names in VALIDATION.md
- Minimal deck fixture exists for integration tests
- Wave 0 requirements in VALIDATION.md are satisfiable
</success_criteria>

<output>
After completion, create `.planning/phases/02-compiler-core-ada/02-00-SUMMARY.md`
</output>
