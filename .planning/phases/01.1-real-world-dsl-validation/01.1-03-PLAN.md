---
phase: 01.1-real-world-dsl-validation
plan: 03
type: execute
wave: 3
depends_on: ["01.1-01", "01.1-02"]
files_modified:
  - tests/fixtures/real_clustering_ch8_tutorial.yaml
  - .planning/gap-report.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - DSL-01

must_haves:
  truths:
    - "Deck 5 (clustering-ch8-tutorial, 30 slides) parses via parse_deck_file() without errors"
    - "All 5 real-world decks parse successfully, proving DSL-01"
    - "Gap report documents layout coverage, gaps found, and schema changes made"
    - "DSL-01 is marked complete in REQUIREMENTS.md"
  artifacts:
    - path: "tests/fixtures/real_clustering_ch8_tutorial.yaml"
      provides: "Deck 5 YAML fixture — heavy raster images, chapter labels"
      contains: "layout: figure"
    - path: ".planning/gap-report.md"
      provides: "Gap analysis for Phase 2 planning"
      contains: "Layout Coverage"
    - path: ".planning/REQUIREMENTS.md"
      provides: "DSL-01 marked complete"
      contains: "[x] **DSL-01**"
  key_links:
    - from: ".planning/gap-report.md"
      to: "tests/fixtures/real_*.yaml"
      via: "Documents validation results from all 5 fixtures"
      pattern: "real_"
---

<objective>
Convert the final deck (Deck 5, 30 slides with heavy raster images), generate the gap report documenting all findings across all 5 decks, and close DSL-01 in REQUIREMENTS.md.

Purpose: Complete the validation sprint. The gap report becomes input for Phase 2 compiler planning.
Output: Final YAML fixture, gap report, DSL-01 closure.
</objective>

<execution_context>
@/Users/erlebach/.claude/get-shit-done/workflows/execute-plan.md
@/Users/erlebach/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01.1-real-world-dsl-validation/01.1-CONTEXT.md
@.planning/phases/01.1-real-world-dsl-validation/01.1-RESEARCH.md
@.planning/phases/01.1-real-world-dsl-validation/01.1-01-SUMMARY.md
@.planning/phases/01.1-real-world-dsl-validation/01.1-02-SUMMARY.md
@schema/models.py

<interfaces>
From schema/models.py (post Plan 01+02 fixes):
- FigureSlide: layout='figure', title, src, alt_text, caption (opt)
- All 12 layout types should have been exercised across Decks 1-4
- parse_deck_file() is the validation entry point

Gap report template from RESEARCH.md:
- Decks Validated table (File, Slides, Status, Notes)
- Layout Coverage table (Layout Type, Times Used, Decks)
- Gaps Found section with HTML pattern, closest DSL type, decision, schema change
- Schema Changes Made section
- DSL-01 Closure checklist
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert Deck 5 (clustering-ch8-tutorial, 30 slides)</name>
  <files>tests/fixtures/real_clustering_ch8_tutorial.yaml</files>
  <read_first>
    - ../n8n_to_python/output_html_files/clustering-ch8-prototype-tutorial-opus46-2026-03-20_2008.html
    - schema/models.py
    - tests/fixtures/real_prototype_clustering_tutorial.yaml
  </read_first>
  <action>
1. Read the HTML deck: 30 slides with heavy raster images (22), slide--title/slide--content CSS class scheme, chapter labels, table.

2. Create `tests/fixtures/real_clustering_ch8_tutorial.yaml`:
   - Deck metadata: title from HTML, author=null, date=2026-03-20, theme=dark
   - Layout mapping:
     - `slide--title` -> layout: title
     - Slides with `<img>` (22 of them) -> layout: figure with src=image filename, alt_text from HTML alt attribute or descriptive text. For slides where the image is supplementary to text content, use layout: content and mention the image in notes field
     - `<div class="chapter-label">` -> Drop or include text in notes field (per RESEARCH.md: not significant content loss)
     - `<table>` -> layout: comparison or content
     - Standard content slides -> layout: content
     - Closing -> layout: summary
   - For the 22 raster images: not every image-containing slide should be `figure`. Use `figure` when the image IS the slide content. Use `content` when the image accompanies text and the text is primary
   - Validate through parse_deck_file() after writing

3. Run `python -m pytest tests/test_real_world_decks.py -v` — should show 5 fixtures passing.
  </action>
  <verify>
    <automated>cd /Users/erlebach/src/2026/claude-code/n8n/md_to_yaml && python -m pytest tests/test_real_world_decks.py -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/fixtures/real_clustering_ch8_tutorial.yaml exists
    - tests/fixtures/real_clustering_ch8_tutorial.yaml contains `layout: figure`
    - tests/fixtures/real_clustering_ch8_tutorial.yaml contains `alt_text:`
    - `python -m pytest tests/test_real_world_decks.py -v` shows 5 fixtures, exits 0
  </acceptance_criteria>
  <done>All 5 real-world decks parse without errors</done>
</task>

<task type="auto">
  <name>Task 2: Generate gap report + close DSL-01</name>
  <files>.planning/gap-report.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - tests/fixtures/real_quantum_transformers_impl.yaml
    - tests/fixtures/real_quixer_implementation.yaml
    - tests/fixtures/real_prototype_clustering_tutorial.yaml
    - tests/fixtures/real_density_clustering.yaml
    - tests/fixtures/real_clustering_ch8_tutorial.yaml
    - schema/models.py
  </read_first>
  <action>
1. Create `.planning/gap-report.md` following the template from RESEARCH.md:

   **Decks Validated table:** List all 5 decks with slide count, PASS/FAIL status, and notes about notable patterns or issues encountered.

   **Layout Coverage table:** Count how many times each of the 12 layout types appears across all 5 fixtures. Identify any layout types with zero usage (quote is expected to have zero — note this).

   **Gaps Found section:** For each HTML pattern that required a judgment call during conversion:
   - HTML pattern (e.g., card-grid, formula-block, chapter-label, inline SVG, tag-row, inline styles)
   - Closest DSL type used
   - Decision made (mapped to X because...)
   - Schema change made (None or description)

   **Schema Changes Made section:** List every change to models.py or parser.py made during Plans 01-03. If none were needed, state "No schema changes required — all 12 layout types and existing fields were sufficient."

   **DSL-01 Closure checklist:**
   - [x] All 5 decks parse without errors
   - [x] All 12 layout types exercised (or note which weren't and why)
   - [x] REQUIREMENTS.md DSL-01 marked complete

2. Update `.planning/REQUIREMENTS.md`:
   - Change `- [ ] **DSL-01**:` to `- [x] **DSL-01**:`
   - In Traceability table, change DSL-01 Status from `Pending` to `Complete`

3. Run final full test suite: `python -m pytest tests/ -v`
  </action>
  <verify>
    <automated>cd /Users/erlebach/src/2026/claude-code/n8n/md_to_yaml && grep -c "\[x\] \*\*DSL-01\*\*" .planning/REQUIREMENTS.md && grep "Layout Coverage" .planning/gap-report.md && python -m pytest tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - .planning/gap-report.md exists and contains "## Decks Validated"
    - .planning/gap-report.md contains "## Layout Coverage"
    - .planning/gap-report.md contains "## Gaps Found"
    - .planning/gap-report.md contains "## DSL-01 Closure"
    - .planning/REQUIREMENTS.md contains `[x] **DSL-01**`
    - .planning/REQUIREMENTS.md Traceability table shows DSL-01 as Complete
    - `python -m pytest tests/ -v` exits 0
  </acceptance_criteria>
  <done>Gap report generated, DSL-01 marked complete, full test suite green, phase 01.1 complete</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -v` passes (full suite green)
- All 5 real-world fixtures discovered by parametrized test
- Gap report exists with layout coverage, gaps, and schema changes
- DSL-01 marked complete in REQUIREMENTS.md
</verification>

<success_criteria>
- 5 real-world YAML fixture files in tests/fixtures/
- Gap report in .planning/gap-report.md
- DSL-01 closed in REQUIREMENTS.md
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-real-world-dsl-validation/01.1-03-SUMMARY.md`
</output>
