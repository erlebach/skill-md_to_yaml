---
phase: 01.1-real-world-dsl-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_real_world_decks.py
  - tests/fixtures/real_quantum_transformers_impl.yaml
  - tests/fixtures/real_quixer_implementation.yaml
  - schema/models.py
  - schema/parser.py
autonomous: true
requirements:
  - DSL-01

must_haves:
  truths:
    - "Deck 1 (quantum-transformers, 8 slides) parses via parse_deck_file() without errors"
    - "Deck 3 (quixer-implementation, 11 slides) parses via parse_deck_file() without errors"
    - "Parametrized test auto-discovers real_*.yaml fixtures and validates each"
    - "Any schema/parser issues found during conversion are fixed in models.py/parser.py"
  artifacts:
    - path: "tests/test_real_world_decks.py"
      provides: "Parametrized test for real-world deck validation"
      contains: "pytest.mark.parametrize"
    - path: "tests/fixtures/real_quantum_transformers_impl.yaml"
      provides: "Deck 1 YAML fixture"
      contains: "layout: code"
    - path: "tests/fixtures/real_quixer_implementation.yaml"
      provides: "Deck 3 YAML fixture"
      contains: "layout: steps"
  key_links:
    - from: "tests/test_real_world_decks.py"
      to: "schema/parser.py"
      via: "parse_deck_file() import"
      pattern: "from schema.parser import parse_deck_file"
    - from: "tests/test_real_world_decks.py"
      to: "tests/fixtures/real_*.yaml"
      via: "Path.glob discovery"
      pattern: "glob.*real_.*yaml"
---

<objective>
Create the parametrized test infrastructure and convert the first 2 HTML decks (Deck 1: quantum-transformers 8 slides, Deck 3: quixer-implementation 11 slides) to YAML DSL format. Fix any schema or parser issues discovered during conversion.

Purpose: Establish the test framework and validate the DSL against real content with code blocks, SVG diagrams, steps, two-column, and card-grid patterns. Schema fixes here benefit all subsequent deck conversions.
Output: 2 YAML fixture files, 1 test file, and any schema/parser fixes needed.
</objective>

<execution_context>
@/Users/erlebach/.claude/get-shit-done/workflows/execute-plan.md
@/Users/erlebach/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-real-world-dsl-validation/01.1-CONTEXT.md
@.planning/phases/01.1-real-world-dsl-validation/01.1-RESEARCH.md
@schema/models.py
@schema/parser.py
@tests/fixtures/valid_deck.yaml

<interfaces>
<!-- Key types and contracts from existing codebase -->

From schema/models.py:
- SlideBase: layout, title, notes (optional), body (optional, excluded from JSON Schema)
- TitleSlide: layout='title', title, subtitle (opt), author (opt)
- HeroSlide: layout='hero', title, description (opt)
- ContentSlide: layout='content', title
- DividerSlide: layout='divider', title
- FigureSlide: layout='figure', title, src, alt_text, caption (opt)
- DiagramSlide: layout='diagram', title, alt_text
- TwoColumnSlide: layout='two-column', title, left (opt ColumnContent), right (opt ColumnContent), proportion (50/50|40/60|60/40)
- ColumnContent: type ('figure'|'diagram'), src (opt), alt_text
- QuoteSlide: layout='quote', title, quote, attribution (opt)
- ComparisonSlide: layout='comparison', title
- CodeSlide: layout='code', title, language (opt)
- StepsSlide: layout='steps', title
- SummarySlide: layout='summary', title
- All models: extra='forbid', body field set by parser not YAML

From schema/parser.py:
- parse_deck_file(path: str | Path) -> Deck
- _looks_like_frontmatter(text: str) -> bool
- body: field content comes from Markdown below closing --- of each slide's frontmatter
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create parametrized test + convert Deck 1 (quantum-transformers, 8 slides)</name>
  <files>tests/test_real_world_decks.py, tests/fixtures/real_quantum_transformers_impl.yaml</files>
  <read_first>
    - ../n8n_to_python/output_html_files/quantum-transformers-implementation-svg-opus46-2026-03-19_1941.html
    - schema/models.py
    - schema/parser.py
    - tests/fixtures/valid_deck.yaml
  </read_first>
  <action>
1. Create `tests/test_real_world_decks.py` with:
   - Import `parse_deck_file` from `schema.parser`
   - Import `Path` from `pathlib`, `pytest`
   - `FIXTURES_DIR = Path(__file__).parent / 'fixtures'`
   - `REAL_WORLD_FIXTURES = sorted(FIXTURES_DIR.glob('real_*.yaml'))`
   - Parametrized test `test_real_world_deck_parses(deck_path)` that calls `parse_deck_file(deck_path)`, asserts `len(deck.slides) > 0` and `deck.metadata.title`
   - Parametrized test `test_real_world_deck_layout_types_valid(deck_path)` that parses deck and asserts every `slide.layout` is not None

2. Create `tests/fixtures/real_quantum_transformers_impl.yaml` by reading the HTML deck and converting each slide:
   - Read the HTML file: 8 slides with Python code blocks (4), SVG diagrams (5), two-col, card-grid, tables
   - Deck metadata: title="Quantum Transformers: Architecture and Implementation", author=null, date=2026-03-19, theme=dark
   - For each HTML section, determine the closest DSL layout type using the mapping from RESEARCH.md:
     - Opening hero/title section -> layout: title
     - Sections with `<pre><code class="language-python">` -> layout: code, language: python
     - Sections with inline `<svg>` -> layout: diagram, alt_text describing the diagram, body contains Mermaid placeholder text (NOT raw SVG, per Pitfall 3 in RESEARCH.md)
     - Sections with `two-col` class -> layout: two-column, set proportion based on class (50/50 default, 40/60 for nw variants)
     - Sections with `card-grid` -> layout: content (per CONTEXT.md: cards are decorative, map to bullets)
     - Sections with `<table>` -> layout: comparison (for 2-col tables) or layout: content (for 3+ col tables) with Markdown table in body
     - Closing summary/recap section -> layout: summary
   - Markdown body goes BELOW the closing `---` of each slide's frontmatter (NOT as `body:` in YAML)
   - KaTeX `$$...$$` blocks preserved as-is in Markdown body
   - Validate through `parse_deck_file()` after writing, fix any validation errors immediately

3. Run `python -m pytest tests/test_real_world_decks.py -v` to confirm Deck 1 parses.
   If schema/parser issues found, fix them in models.py/parser.py and run full suite `python -m pytest tests/ -v` to ensure no regressions.
  </action>
  <verify>
    <automated>cd /Users/erlebach/src/2026/claude-code/n8n/md_to_yaml && python -m pytest tests/test_real_world_decks.py -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_real_world_decks.py contains `pytest.mark.parametrize`
    - tests/test_real_world_decks.py contains `from schema.parser import parse_deck_file`
    - tests/test_real_world_decks.py contains `glob('real_*.yaml')`
    - tests/fixtures/real_quantum_transformers_impl.yaml exists and contains `layout: title`
    - tests/fixtures/real_quantum_transformers_impl.yaml contains `layout: code`
    - tests/fixtures/real_quantum_transformers_impl.yaml contains `language: python`
    - `python -m pytest tests/test_real_world_decks.py -v` exits 0
    - `python -m pytest tests/ -v` exits 0 (no regressions)
  </acceptance_criteria>
  <done>Deck 1 fixture parses without errors, parametrized test framework works, full test suite green</done>
</task>

<task type="auto">
  <name>Task 2: Convert Deck 3 (quixer-implementation, 11 slides)</name>
  <files>tests/fixtures/real_quixer_implementation.yaml</files>
  <read_first>
    - ../n8n_to_python/output_html_files/quixer-implementation-svg-opus46-2026-03-19_2136.html
    - schema/models.py
    - tests/fixtures/real_quantum_transformers_impl.yaml
  </read_first>
  <action>
1. Read the HTML deck: 11 slides with step-list, card-grid, two-col, formula, tables, SVGs.

2. Create `tests/fixtures/real_quixer_implementation.yaml` by converting each slide:
   - Deck metadata: title from the HTML `<title>` or opening slide, author=null, date=2026-03-19, theme=dark
   - Apply same mapping rules as Task 1:
     - `step-list` divs -> layout: steps, numbered items as Markdown ordered list in body
     - `card-grid` -> layout: content with card content as bullet lists
     - `two-col` -> layout: two-column with appropriate proportion
     - `formula-block` with `$$...$$` -> layout: content with KaTeX in body (per Pitfall 4)
     - `<table>` -> layout: comparison or content depending on column count
     - Inline SVG -> layout: diagram with alt_text and placeholder body
     - Summary/closing -> layout: summary
   - Markdown body below closing `---`, never as `body:` in YAML frontmatter
   - Validate through `parse_deck_file()` after writing, fix any errors immediately

3. If any new schema/parser issues found (distinct from Task 1 fixes), fix in models.py/parser.py and run full suite.

4. Run `python -m pytest tests/test_real_world_decks.py -v` — both Deck 1 and Deck 3 should now pass.
  </action>
  <verify>
    <automated>cd /Users/erlebach/src/2026/claude-code/n8n/md_to_yaml && python -m pytest tests/test_real_world_decks.py -v && python -m pytest tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/fixtures/real_quixer_implementation.yaml exists and contains `layout: steps`
    - tests/fixtures/real_quixer_implementation.yaml contains `layout: two-column`
    - `python -m pytest tests/test_real_world_decks.py -v` shows 2 fixtures discovered (real_quantum, real_quixer)
    - `python -m pytest tests/ -v` exits 0
  </acceptance_criteria>
  <done>Deck 3 fixture parses without errors, both real-world fixtures pass, full suite green</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_real_world_decks.py -v` passes with 2 deck fixtures
- `python -m pytest tests/ -v` passes (no regressions from any schema changes)
- Both YAML fixtures contain diverse layout types (code, diagram, steps, two-column, content, title, summary)
</verification>

<success_criteria>
- 2 real-world YAML fixture files exist in tests/fixtures/
- Parametrized test discovers and validates both
- Full test suite green
- Schema/parser issues fixed (if any found)
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-real-world-dsl-validation/01.1-01-SUMMARY.md`
</output>
