---
phase: 01.1-real-world-dsl-validation
plan: 02
type: execute
wave: 2
depends_on: ["01.1-01"]
files_modified:
  - tests/fixtures/real_prototype_clustering_tutorial.yaml
  - tests/fixtures/real_density_clustering.yaml
autonomous: true
requirements:
  - DSL-01

must_haves:
  truths:
    - "Deck 2 (prototype-clustering-tutorial, 27 slides) parses via parse_deck_file() without errors"
    - "Deck 4 (density-based-clustering, 15 slides) parses via parse_deck_file() without errors"
  artifacts:
    - path: "tests/fixtures/real_prototype_clustering_tutorial.yaml"
      provides: "Deck 2 YAML fixture — raster images, divider, formula-block, figure"
      contains: "layout: divider"
    - path: "tests/fixtures/real_density_clustering.yaml"
      provides: "Deck 4 YAML fixture — two-col-nw, card-grid, formula, table"
      contains: "proportion:"
  key_links:
    - from: "tests/test_real_world_decks.py"
      to: "tests/fixtures/real_*.yaml"
      via: "Path.glob auto-discovery"
      pattern: "glob.*real_.*yaml"
---

<objective>
Convert Decks 2 and 4 to YAML DSL format, leveraging schema fixes from Plan 01. Deck 2 (27 slides) exercises raster images, divider, and figure layouts heavily. Deck 4 (15 slides) exercises two-column proportion variants and card-grid patterns.

Purpose: Expand real-world coverage to 4 decks, exercising figure, divider, and proportion-variant layouts not heavily tested in Plan 01.
Output: 2 YAML fixture files that auto-integrate with existing parametrized test.
</objective>

<execution_context>
@/Users/erlebach/.claude/get-shit-done/workflows/execute-plan.md
@/Users/erlebach/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01.1-real-world-dsl-validation/01.1-CONTEXT.md
@.planning/phases/01.1-real-world-dsl-validation/01.1-RESEARCH.md
@.planning/phases/01.1-real-world-dsl-validation/01.1-01-SUMMARY.md
@schema/models.py
@schema/parser.py

<interfaces>
From schema/models.py (post Plan 01 fixes):
- FigureSlide: layout='figure', title, src, alt_text, caption (opt)
- DividerSlide: layout='divider', title
- TwoColumnSlide: layout='two-column', title, left (opt), right (opt), proportion ('50/50'|'40/60'|'60/40')
- ColumnContent: type ('figure'|'diagram'), src (opt), alt_text
- ComparisonSlide: layout='comparison', title
- All models: extra='forbid', body set by parser

From schema/parser.py:
- parse_deck_file(path: str | Path) -> Deck
- body content goes BELOW closing --- of each slide's frontmatter
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert Deck 2 (prototype-clustering-tutorial, 27 slides)</name>
  <files>tests/fixtures/real_prototype_clustering_tutorial.yaml</files>
  <read_first>
    - ../n8n_to_python/output_html_files/prototype-clustering-tutorial-opus46-2026-03-20_2049.html
    - schema/models.py
    - tests/fixtures/real_quantum_transformers_impl.yaml
  </read_first>
  <action>
1. Read the HTML deck: 27 slides with raster images (5), SVGs, formula-block, slide--divider, slide--figure classes, tables, divider.

2. Create `tests/fixtures/real_prototype_clustering_tutorial.yaml`:
   - Deck metadata: title from HTML, author=null, date=2026-03-20, theme=dark
   - Layout mapping:
     - `slide--title` / `slide-hero` -> layout: title
     - `slide--divider` -> layout: divider
     - `slide--figure` with `<img>` -> layout: figure, src=image filename, alt_text from `<img alt="...">` or descriptive text
     - Inline SVG sections -> layout: diagram with descriptive alt_text and placeholder body
     - `formula-block` with `$$...$$` -> layout: content with KaTeX preserved in body
     - `<table>` sections -> layout: comparison or content
     - Standard content sections -> layout: content
     - Closing/recap -> layout: summary
   - For raster images: set `src` to the image filename (e.g., "clustering_example.png"), set `alt_text` from the HTML `alt` attribute or write descriptive alt text
   - Markdown body below closing `---`
   - Validate through parse_deck_file() after writing

3. Run `python -m pytest tests/test_real_world_decks.py -v` — should show 3 fixtures passing.
   If new schema issues found, fix and run full suite.
  </action>
  <verify>
    <automated>cd /Users/erlebach/src/2026/claude-code/n8n/md_to_yaml && python -m pytest tests/test_real_world_decks.py -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/fixtures/real_prototype_clustering_tutorial.yaml exists
    - tests/fixtures/real_prototype_clustering_tutorial.yaml contains `layout: figure`
    - tests/fixtures/real_prototype_clustering_tutorial.yaml contains `layout: divider`
    - tests/fixtures/real_prototype_clustering_tutorial.yaml contains `alt_text:`
    - `python -m pytest tests/test_real_world_decks.py -v` shows 3 fixtures, exits 0
  </acceptance_criteria>
  <done>Deck 2 (27 slides) parses without errors, 3 real-world fixtures pass</done>
</task>

<task type="auto">
  <name>Task 2: Convert Deck 4 (density-based-clustering, 15 slides)</name>
  <files>tests/fixtures/real_density_clustering.yaml</files>
  <read_first>
    - ../n8n_to_python/output_html_files/density-based-clustering-explanatory-opus46-2026-03-20_1326.html
    - schema/models.py
    - tests/fixtures/real_prototype_clustering_tutorial.yaml
  </read_first>
  <action>
1. Read the HTML deck: 15 slides with two-col-nw (narrow-wide proportion variant), card-grid, formula, table, SVGs.

2. Create `tests/fixtures/real_density_clustering.yaml`:
   - Deck metadata: title from HTML, author=null, date=2026-03-20, theme=dark
   - Layout mapping:
     - `two-col-nw` or `two-col-wide-narrow` -> layout: two-column with `proportion: '40/60'` or `proportion: '60/40'` (match the HTML intent, per Pitfall 5)
     - `card-grid` -> layout: content with card content as bullets (per CONTEXT.md: don't add new layout type)
     - `formula-block` -> layout: content with `$$...$$` in body
     - `<table>` -> layout: comparison or content
     - Inline SVG -> layout: diagram with alt_text and placeholder body
     - Standard content -> layout: content
     - Opening -> layout: title, closing -> layout: summary
   - Explicitly set `proportion:` on two-column slides, do NOT rely on 50/50 default when HTML uses unequal columns
   - Validate through parse_deck_file() after writing

3. Run full test suite: `python -m pytest tests/ -v`
  </action>
  <verify>
    <automated>cd /Users/erlebach/src/2026/claude-code/n8n/md_to_yaml && python -m pytest tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/fixtures/real_density_clustering.yaml exists
    - tests/fixtures/real_density_clustering.yaml contains `layout: two-column`
    - tests/fixtures/real_density_clustering.yaml contains `proportion:` (at least one non-default proportion)
    - `python -m pytest tests/test_real_world_decks.py -v` shows 4 fixtures, exits 0
    - `python -m pytest tests/ -v` exits 0
  </acceptance_criteria>
  <done>Deck 4 (15 slides) parses without errors, 4 real-world fixtures pass, full suite green</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_real_world_decks.py -v` passes with 4 deck fixtures
- `python -m pytest tests/ -v` passes (full suite green)
- Both fixtures contain layout types not heavily exercised in Plan 01 (figure, divider, two-column with proportion)
</verification>

<success_criteria>
- 4 real-world YAML fixture files total in tests/fixtures/
- All 4 auto-discovered and validated by parametrized test
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-real-world-dsl-validation/01.1-02-SUMMARY.md`
</output>
