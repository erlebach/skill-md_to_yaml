# HANDOFF — 2026-06-12 13h41mEST

**Focus for the next session:** the user is visually testing every fixture deck via `generate_html_files.sh`; continue that QA loop — compile, inspect screenshots, fix renderer or fixture issues as they surface — on branch `fixed-canvas-generator`.

## Read first / references

- **`docs/superpowers/plans/2026-06-11-fixed-canvas-slide-generator.md`** — the master plan. Phase 1 (fixed 1280×720 canvas in the generator) is complete; Phase 2 (font embedding 2.1, build-time Mermaid bake 2.2) and Phase 3 (retire `migrate_deck.py`) remain open. Task 2.2's reference implementation is `zoom_independence/bake_mermaid.py`.
- **`generate_html_files.sh`** — the compile-all loop the user is running (outputs HTML to repo root; negative fixtures excluded by design).
- **`.claude/skills/md_to_yaml/compiler/templates/base.html.j2`** — all rendering logic (frozen-px CSS + sizing/autofit JS). The single source; `schema/` and `compiler/` live only under `.claude/skills/md_to_yaml/` now (root copies and `standalone/` are gone).
- **`tests/measure_two_col_mermaid.py`** — reusable headless geometry probe (pytest ignores it).
- Commits this session: `74b3612`, `e727ecf`, `cdb9fd5`, `3e6d8ed` (2026-06-11) and `5d7861f`, `aaf5df2`, `001d44b`, `ab32c45` (2026-06-12) — see bodies for detail.

## What changed this session

- **`5d7861f`** — the big compiler commit: figure-only pinned panel (absolute, fills content area to the one-inch margin), comparison-slide panel + geometric centering, autofit extended to require the content area to contain its children, Mermaid sizing measures the *real* panel/column (`PANEL_FILL = 0.95`), `mermaid_scale` honored in measured mode, new per-slide `panel_scale` on `layout: diagram` (`float` or `[x, y]`, 0.2–1.0 → `--panel-scale-x/y`), and the sizing↔autofit ordering fix (reset zoom → size → autofit last). `deck.schema.json` regenerated after being long stale.
- **`aaf5df2`** — table cells/titles starting with `>` or `#` render literally (were becoming blockquote/heading).
- **`001d44b`** — all fixtures conform to deck recommendations (bullet ≤8, no 3+ same-layout runs) content-preservingly; raw-Mermaid bodies made contiguous (blank line = source/caption split per `engine.py:399`); `minimal_deck` and `valid_deck` repaired.
- **2026-06-11 commits** — two-column Mermaid panel fill + `elScale()` (sees autofit zoom via rect÷offsetWidth), code-gutter line metrics, fixture asset resolution, `standalone/` removal.
- **Verification:** suite green at 179 (`PYTHONPATH=.claude/skills/md_to_yaml .venv/bin/python -m pytest -q`); headless probes confirmed bidirectional svg-in-panel containment on all five diagram slides of `aria_compliance_v3`.

## Decisions locked

- **Template directory is ground truth for layouts** — a `layout:` without `templates/<name>.html.j2` is invalid regardless of docs. (`SKILL.md`'s list omits `figure-wide` — known doc gap, unfixed.)
- **Mermaid stays runtime-rendered for now** — baking to SVG is Phase 2 Task 2.2, deliberately deferred, not dropped. Pre-rendered `.svg` via `src:` is the determinism workaround today.
- **`mermaid_scale` is clamped to panel fit** — no author value may overrun a panel; defaults must never overrun (user requirement, stated twice).
- **Panel fill policy is 0.95** (`PANEL_FILL`), agreed with the user; panel inset is a fraction, not pixel arithmetic.
- **Sizing runs at autofit zoom 1, autofit gets the final say** — any future sizing pass must preserve this ordering or the inverse-box inflation bug returns.
- **`two_column_variants.yaml` keeps its variety warning** — its test contract (`test_parser.py:143`) requires exactly 4 consecutive two-column slides.
- **Scale floors:** `content_scale`/`title_scale` ≥ 0.75 (legibility guard; autofit handles overflow instead).

## Next steps

1. Continue fixture-by-fixture visual QA (the user drives; screenshots arrive in chat). Re-run `bash generate_html_files.sh` after each fix — zero warnings expected except `two_column_variants`.
2. Consider extending `panel_scale` to figure panels / two-column columns (user hinted at "cases needing special treatment"; offered, not yet requested).
3. Possible engine improvement (offered, undecided): smarter raw-Mermaid source/caption split (consume indented/`style` lines instead of hard blank-line split).
4. Phase 2 Task 2.2 (`--bake-mermaid` compile flag) when cross-device determinism becomes the priority again.
5. Scratch cleanup candidates (untracked, **not** committed; confirm with user before deleting): compiled `*.html` at repo root and in `tests/fixtures/`, `tests/fixtures/output_html_files`.

## Constraints still in force

- Run Python via the project venv: `PYTHONPATH=.claude/skills/md_to_yaml .venv/bin/python -m …` (or `uv run` outside the sandbox; the sandbox blocks `~/.cache/uv` and writes under `.claude/skills` — Playwright/browser verification needs unsandboxed Bash).
- The freeze oracle (`test_no_fluid_units_in_output`) scans CSS **comments** too — never write `1in`-style tokens anywhere in the template `<style>`.
- Raw Mermaid in diagram bodies must be one contiguous block (blank line starts the caption).
- Fixtures: only documented layouts/fields; every `src:` must resolve (assets live in `tests/fixtures/`); negative fixtures (`invalid_*`) must stay invalid and out of compile scripts.
- Surgical diffs (Karpathy guidelines); JOURNAL prepend-first with `HH:MM` headers + summary block (read `~/.claude/memory/feedback/journal_automation.md` before writing entries); timestamps `HHhMMmEST`.

## Suggested skills

- **mgrep** — semantic search over this repo/docs before reading files (user-mandated for all searches).
- **andrej-karpathy-skills:karpathy-guidelines** — before any code edit; the user invokes it by name when diffs grow.
- **playwright-cli** (or inline Playwright via venv, as this session did) — headless screenshot/geometry verification of compiled decks; it is the only trustworthy way to check sizing claims.
- **git** (`/git`) — the user commits in logical groups frequently; follow the conventional-commit + trailer format.
