# Slide deck compiler (YAML → HTML) bundled with this skill.
# Install:  pip install -r requirements.txt
# Run from the skill directory with PYTHONPATH set to this folder (see SKILL.md).

pydantic>=2.12
PyYAML>=6.0
jinja2>=3.1
latex2mathml>=3.79.0
Pygments>=2.19.2
Markdown>=3.10.2
defusedxml>=0.7.1
bleach>=6.3.0
