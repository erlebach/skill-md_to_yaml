<header class="slide-heading-stack slide-heading-stack--divider">
  <h2 id="{{ slide_id }}-heading">{{ rendered_title | safe }}</h2>
  {% if rendered_subtitle %}<p class="slide-subtitle">{{ rendered_subtitle | safe }}</p>{% endif %}
</header>
