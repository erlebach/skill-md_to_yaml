[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "vtt-polisher"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["anthropic>=0.25"]

[project.scripts]
vtt-polish = "vtt_polisher.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
